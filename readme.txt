20141113:
1. 修改中断优先级分配，timer为（0,0）（0,1）两个timer，串口为（1,0）。后续加入rf发送中断用到timer设为（0,2）
2. 中断文件里增加调用语句，将中断服务函数本体放到用户功能模块中。
3. 加axf转bin脚本。


20141120:
1. 把adc求平均值里误差允许范围从4调到6，因为报警开关按下时，误差比4大（现象如此）。
2. 将短路合并到正常状态
	传感器有紧急按钮报警传感器和有线门磁传感器。
     (1)紧急按钮不按adc值 1380~ 1500经常是1437,1438
	紧急按钮按下：>4000  经常是4095
	紧急按钮不连接：经常4095
     (2)门磁合上：<20，经常是0，用镊子短路二线，值也是0
	门磁打开：>4000，经常是4094,4095
	门磁不连接：经常4095

3. 二线传感器状态变化后，上传状态变化消息，等待回应确认，若没有收到回应消息，二秒后重新上传。

4. 串口通信不准备将接收和发送放在一个文件里做，太麻烦，发送命令只有几条而已。
而且sensor检测死循坏5次，再集中发送串口信息，，修改太麻烦。

5. 串口后台中断加上  允许发送和允许接收标记，为了避免发送和接收同时触发。（之前已经有加，不用做）

20141125:
+-------------------------------------------------------------------------------+
1. 写串口前台主循坏，协议未定，命令格式调试。

2. 修改sensor检测主循坏，本来为一次性检测所有sensor再发upload命令，修改为每次检测判断需不需要upload。

3. 将状态灯改为1s闪烁，将发送rf信号灯处理放在rf发送函数里

4. 写个获取当前传感器状态的接口，可以选择任一传感器；
+-------------------------------------------------------------------------------+
20141201:
1. 加多个串口调试process；
2. 修改rf发送函数bug；
3. 检测传感器状态上传命令暂时屏蔽。
+-------------------------------------------------------------------------------+
20141202:
1. 状态灯和rf信号灯。合二为1
+-------------------------------------------------------------------------------+

20141208: 未测试
1. 上传传感器命令，重发3次就停止重发；
2. 修改上传事件初始复位函数bug；
+-------------------------------------------------------------------------------+

20141209:
1. hw_config.h: learn led modify config in PIN PD7 .
2. dial switch config add 2pin in nodeaddr, pin PD1,PD2
3. appsensor.c: modify #define 
4. appusart.c: add record smart socket operation
5. osrf315tx.c: add record smart socket operation fun;add get smart socket status;

+-------------------------------------------------------------------------------+
  